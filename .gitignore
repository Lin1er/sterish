# Rust
target/
/target/
contracts/target/
**/*.rs.bk
*.wasm
.soroban/
.stellar/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
.next/
out/
*.tsbuildinfo
.eslintcache

# Environment — only *.env.example is tracked. Never commit a real secret key.
# (see CLAUDE.md 'Secret & wallet key')
.env
.env.keys
.env.local
.env.*.local
*/.env

# Local service state
*.db
*.db-journal
*.sqlite
*.sqlite3

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Misc
*.log
coverage/

test_snapshots/

# Corpus audit run artifact
pipeline/corpus/last-run.json

# x402 demo buyer deps (STE-19)
demo/*/node_modules/

# Deployment artifacts served by /use — operator-supplied, not repo content (STE-25)
deploy/artifacts/

# Cloudflare Tunnel credentials — the tunnel UUID is public, this is not (STE-25)
deploy/secrets/

# Agent skill installs (npx skills add) - local tooling, not repo content
.agents/
.claude/
skills-lock.json

# Orchestrator run journal. Local bookkeeping for a seed run, not evidence —
# the evidence is the reports and the ledger. See docs/audit-evidence.md.
.sterish-journal.json
